syntax = "proto3";

package raft;

service Raft {
  rpc AppendEntries(Term) returns (Empty) {}
  rpc RequestVote(Term) returns (VoteReply) {}
}

message Empty {}

message Term {
  int32 term = 1;
}

message VoteReply {
  bool yes = 1;
}
